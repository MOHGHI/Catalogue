{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-group">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="Сохранить">
        {{ form_end(form) }}
    </div>
{% endblock %}